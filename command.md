# 1
# Версия
git --version
# Выведите историю проекта через команду 
git log
# Добавление изменение имени
# Переключитесь на нужный коммит
git checkout хеш коммита
git config --global user.name "Emma Paris"
# Добавление изменение адреса
git config --global user.email "eparis@atlassian.com"
# Проверка настроек
git config --global --list
# Настройка формата окончания строк
git config --global core.autocrlf true
git config --global core.safecrlf warn
git config --global core.quotepath off
# Задайте общепринятое название основной ветки в репозитории в Git — main
git config --global init.defaultBranch main
# Git даёт подсказки со всеми возможными командами
git help -g
git help команда
# 2 
# Чтобы проверить текущее состояние репозитория
git status
# В коммит попадут все файлы из текущей папки
git add -A
# Чтобы добавить файл в отслеживаемые
git add README.md  (где git add — это команда, а README.md — имя файла)
# 
git commit -m "add README"  В кавычках принято писать о том, что изменилось в проекте с последнего снимка.
# Чтобы создать локальный репозиторий
 git init   Проверьте, что появилась папка .git
 # Поместите файл в последний коммит (исправление перезапись)
 git commit --amend -m "error"
# проверить историю проекта
git log --oneline
# покажет всю историю проекта
git log --all --oneline
# Перемещение по истории проекта
git checkout -
# Проверьте, что вернулись к последней версии проекта через команду 
git log –-oneline:

# Чтобы сделать снимок проекта (сформировать коммит)
##Добавьте файл или файлы в отслеживаемые командой
git add имя файла
## Создайте коммит и подпишите его командой
git commit -m "Подпись коммита"
## Чтобы проверить состояние файла
## На любом из этапов вы можете выполнить команду
git status
# Внесение изменений в файл Git
## Добавьте файл в индекс и подпишите коммит, выполнив последовательно 2 команды
git add README.md и git commit -m "change README"
## Вновь проверьте статус файла через команду 
git status. 
Git выдаст сообщение, что ничего в проекте не изменилось с последнего сохранения
# Просмотр истории проекта
git log --oneline
# Чтобы посмотреть всю информацию о коммите
 git log без флFага Q — режим вывода закроется
# Исправление коммита
 git commit --amend -m "error" ("error" испрвленный коммит)

# Связь локального и удалённого репозиториев
## Проверьте, что к папке test ещё не привязан никакой удалённый репозиторий
git remote -v
## Связь локального и удалённого репозиториев
git remote add origin ссылка на папку удалённого репозитория
## Если нам когда-нибудь потребуется удалить связь
git remote remove origin 

# Отправка проекта в удалённый репозиторий
git push -u origin main
# Добавьте и закоммитьте
git add README.md
git commit -m "Первый коммит"

git push -u origin main

# Fork и клонирование репозитория
git clone и вставьте только что скопированную ссылку
# Чтобы продолжить работать с проектом, нам нужно открыть эту папку в терминале
cd и имя папки
# Просмотр истории ветки в терминале Git
git log имя-ветки --graph --oneline


# Создание ветки проекта в Git
## Для работы с ветками существует команда 
git branch
## Чтобы создать новую ветку, нужно набрать 
git branch имя ветки
## переключимся на новую ветку
git checkout и указать имя ветки




# Слияние веток в Git
## Переключитесь обратно на ветку main
git checkout main
чтобы выполнить слияние веток , нужно написать
git merge имя ветки
## Вернитесь на ветку main 
git checkout main
## слияние 
git merge имя ветки
## слить изменения из ветки fix в ветку main через команду слияния 
git merge fix
## тобы создать новую ветку и переключиться на неё, нужна команда
git checkout -b имя-новой-ветки
## Чтобы переключаться между ветками, выполняем команду
git checkout имя-ветки
Для слияния находимся на ветке, куда сливаем, и выполняем
git merge имя-сливаемой-ветки
При разрешении конфликта обязательно удаляем служебные

# Внесите изменения
## 
git status
## Сделайте файл отслеживаемым, добавьте его в индекс командой
git add имя файла
## Зафиксируйте изменения, подпишите коммит командой
git commit -m "описание изменения"
## Отправьте ветку в удалённый репозиторий командой 
git push -u origin имя ветки в которую отправляем

# Задание 1
https://github.com/VadKarM/NeuroStartUp
https://github.com/VadKarM/NeuroStartUp/tree/new-text
https://github.com/VadKarM/git-2-homeworks-merge

# Ключевые действия и команды при командной разработке
## Для обновления командного проекта на GitHub нужно использовать команду
git pull
## Чтобы переключаться к предыдущему состоянию проекта, используйте команду
git checkout хеш коммита
## Посмотреть всю историю проекта можно через команду 
git log --oneline --all
## Чтобы отменить коммит в удалённом репозитории, нужно использовать команду 
git revert хеш коммита для отмены

Чтобы выйти из режима редактирования и сохранить подпись для коммита-отмены,
нажмите Ctrl + X, Y и Enter bkb Esc -> :wq -> Enter


# Настроить .gitignore можно для папки и файла с помощью команды 
git rm --cached имя-файла
